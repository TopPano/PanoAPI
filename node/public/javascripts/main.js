var test = TOPPANO;


// var map = test.readURL();
var map = {
	PanoID: '00000000',
	center: new TOPPANO.LatLng(0, 30),
	zoom: 70,
	canvas: 'container',
	isfbShare: true,
	isState: true
};

test.initMap(map);
